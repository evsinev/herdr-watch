# local helper scripts (may contain secrets) — do not commit
build.sh
run.sh

# build artifacts
backend/target/
frontend/node_modules/
frontend/dist/
docker/quarkus-app/
tray-macos/.build/
tray-macos/*.app/

# misc
**/.DS_Store
*.log
.idea/

# OpenAPI: коммитим только yaml (проверяется в CI на дрейф); json генерится рядом.
docs/api/openapi.json

.env
